services:
  # The Python/FastAPI Backend Service
  - type: web
    name: spectrum-api
    env: python
    rootDir: backend
    buildCommand: |
      pip install -r requirements.txt
      python -m spacy download en_core_web_sm
    startCommand: gunicorn --bind 0.0.0.0:10000 main:app
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.4

  # The React Frontend Service
  - type: static
    name: spectrum-frontend
    # The 'env: static' line that was here has been REMOVED
    rootDir: frontend
    buildCommand: npm install && npm run build
    publishDir: dist
    rewriteRules:
      - source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: spectrum-api
          property: url